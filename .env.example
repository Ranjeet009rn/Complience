# OpenAI API Key for compliance analysis
## Server-side (Node) secrets
OPENAI_API_KEY=your_openai_api_key_here
PORT=3001

# When true, backend will return stubbed responses for OpenAI chat API on error
# to keep the app stable during development or quota issues
DEV_STUB_ON_ERROR=true

## Client-side (Vite) public envs
# WARNING: Anything prefixed with VITE_ is exposed to the browser. Do NOT put secrets here for production.
# If you choose to call OpenAI directly from the frontend for personal/testing use only, set VITE_OPENAI_API_KEY.
# This will expose your key to anyone who can access your app.
#
# When VITE_OPENAI_API_KEY is unset, the frontend will call your backend at `${VITE_API_BASE}/api/...`.

# Optional: Base URL for your backend API (e.g., https://api.example.com). Leave empty to use same origin.
VITE_API_BASE=

# Optional: Direct OpenAI usage from frontend (NOT SAFE for production)
# Setting this makes the browser call OpenAI API directly.
VITE_OPENAI_API_KEY=

# VITE_PUBLIC_SOME_VALUE=
